#include <bits/stdc++.h>
using namespace std;
int main(){
    int n; cin >> n;
    vector <int> vec;
    vector <int> vec2;
    for (int i = 0; i < n; i++){
        int a; cin >> a;
        vec.push_back(a);
        if (a == 0) vec2.push_back(i);
    }
    vector <int> res;
    for (int i = 0; i < vec.size(); i++){
        if (vec)
    }
}

//nao finalizado 